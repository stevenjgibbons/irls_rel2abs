If we have the files *relative_x.txt* and *relative_y.txt*, giving approximate distances in meters between 
events *i* and *j*, then the script *get_relative_values.sh* runs the *irls_rel2abs* program to give the files
*absolute_x.txt* and *absolute_y.txt* and ulitmately the file *DPRK_xy.txt*
